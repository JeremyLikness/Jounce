<UserControl x:Class="EventAggregator.Views.Sender"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:EventAggregator.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot" d:DataContext="{d:DesignInstance vm:SenderViewModel,IsDesignTimeCreatable=True}" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <ComboBox HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10" ItemsSource="{Binding Options}" SelectedItem="{Binding CurrentOption,Mode=TwoWay}"/>
        <Button HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10" Content="Broadcast Option" Grid.Column="1" Command="{Binding OptionChangedCommand}" CommandParameter="{Binding CurrentOption}"/>
        <Button HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10" Content="Throw Error" Grid.Column="2" Command="{Binding ThrowErrorCommand}"/>
    </Grid>
</UserControl>
